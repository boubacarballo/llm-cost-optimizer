[project]
name = "llm-cost-optimizer-workspace"
version = "0.1.0"
description = "Workspace root — ties the service packages together for local dev only. Ships nowhere."
readme = "README.md"
requires-python = ">=3.13"
# No dependencies of its own: the real packages live under services/*.

[tool.uv.workspace]
members = ["services/api", "services/classifier"]
