# Task Classifier Service

This service hosts the DeBERTa prompt-complexity classifier model server.

It is built from the `services/task-classifier` package (module `task_classifier`) and exposes a FastAPI app.
